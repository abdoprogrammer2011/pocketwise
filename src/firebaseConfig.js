/**
 * Convenience re-export so firebaseConfig.js lives at src/ as requested.
 * Put your Firebase keys in src/firebase/config.js
 */
export { auth, db, analytics } from './firebase/config';
export { default } from './firebase/config';
